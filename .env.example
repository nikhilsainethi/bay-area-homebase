# Optional static Pages build variables. Prefer the app's Map settings for a personal key.
# Browser keys are visible in requests; restrict origins and allowed APIs in Google Cloud.
VITE_GOOGLE_MAPS_API_KEY=""
VITE_GOOGLE_MAPS_MAP_ID=""

# Supabase browser configuration (never use a secret/service-role key).
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
